# Node modules
node_modules/
frontend/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment Variables (SECURITY CRITICAL)
.env
.env.local
.env.*.local
.env.production
*.pem
*.key

# Hardhat
artifacts/
cache/
coverage/
coverage.json
typechain-types/

# ZK Proof Artifacts - IGNORE ALL (Docker will rebuild deterministically)
build/
# This ensures builds are always fresh and reproducible

# BUT: If you want to cache the Powers of Tau for faster local dev:
# build/
# !build/pot14_final.ptau  
# (This is safe to cache as it's downloaded from official source)

# Circuit intermediate files
circuits/*.r1cs
circuits/*.sym
circuits/*.wasm
circuits/build/
*.zkey
*.ptau
powers/
input.json

# Generated Verifier (will be regenerated)
contracts/Verifier.sol

# Frontend
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/
frontend/.nuxt/
frontend/.turbo/
frontend/.vercel/

# Testing & Logs
test-result.md
log.md
log1.md
log*.md
*.log
*.tmp
*.bak

# IDE & OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
._.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Docker
.dockerignore
docker-compose.override.yml

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
*.tfvars.json
.terraform.lock.hcl

# Misc
flattened-selected.txt
flattened-selected.md
flatten.sh
updatesecret.sh